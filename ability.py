import random

class Ability:
    def __init__(self, name, max_damage):
        self.name = "Bruce"
        self.max_damage = 10000

        
        self.name = name
        self.max_damage = max_damage

random.randint(2, 7)

import random

class Ability:

if __name__ == "__main__":
    ability = Ability("Debugging Ability", 20)
    print(ability.name)
    print(ability.attack())